{
  "scripts": {
    "build:search": "pagefind --site . --output-path _pagefind --exclude-selectors \".comments-section, .site-header, .site-footer\""
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
